<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="200dp"
              android:layout_height="fill_parent"
              android:orientation="horizontal"
        >
    <ImageView
            android:id="@+id/column_icon"
            android:layout_alignParentLeft="true"
            android:layout_height="25dp"
            android:layout_width="25dp"
            android:src="@drawable/listIcon"
            android:contentDescription="@string/logo_description"
            />

    <TextView android:id="@+id/column_label"
              android:layout_height="wrap_content"
              android:layout_width="wrap_content"
              android:text="@string/default_column_label"
              android:layout_alignLeft="@+id/column_icon"
              android:paddingLeft="25dp"
              android:paddingTop="4dp"
              />

    <ListView android:id="@+id/tasks"
              android:layout_height="wrap_content"
              android:layout_width="200dp"
              android:layout_below="@+id/column_icon"

            />
</RelativeLayout>